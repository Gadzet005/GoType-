import { millisecond } from "@desktop-common/types";

// 120 fps
export const TICK_TIME: millisecond = 8.3;

// Amount of score for actions in the game
export namespace Score {
    export const LETTER: number = 10;
}
